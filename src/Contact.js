import React from 'react';
import ReactDOM from 'react-dom/client';
import reportWebVitals from './reportWebVitals';
import {Contactt,Contt} from './Cont.js';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <div>
    <Contactt/>
    </div>
  </React.StrictMode>
);
reportWebVitals();